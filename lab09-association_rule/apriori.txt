support(for a product p) = users that brought P / total number of transactions
confidence(for a product p1 --> p2) = transactions that have p1 and p2 / transactions that have p1
Lift(for a product p1 --> p2) = confidence(for a product p1 --> p2) / support(for a product p2)